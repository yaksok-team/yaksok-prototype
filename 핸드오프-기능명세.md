# YakSok 기능 명세 — Tab 1 · Tab 2 · 등록 (v1)

## § 0 Context

본 문서는 YakSok 와이어프레임([docs/components/senior-screens.jsx](docs/components/senior-screens.jsx), 44 컴포넌트 — 활성 38 + EXPLORE TabBar 데모 6) + IA 모델([핸드오프-IA모델.md](핸드오프-IA모델.md)) + PRD([../YakSok_PRD.md](../YakSok_PRD.md))를 받아, 개발팀이 **Tab 1(오늘) · Tab 2(내 약) · 등록 flow**를 구현할 때 필요한 데이터·상태·계약·화면 레벨 명세를 모은다. 1차 페르소나는 박정숙 68세(시니어).

### Scope (포함)

| 범위 | 컴포넌트 |
|---|---|
| Tab 1 — 오늘 | S_Today (empty/checked/collapsed variants), S_StockCheck |
| Tab 2 — 내 약 | S_Cabinet (empty), S_RxDetail (sheet), S_RxEdit (sheet), S_RxDelete (alert) |
| 등록 시나리오 ① 약 등록 (11 entries) | S_AddType, S_AddChoice, S_Camera, S_Confirm, S_AnalyzeLoading(`kind=med`), S_Result(`kind=med`), S_Schedule(`kind=med`), S_TimeSheet, S_DaySheet, S_AddNote(`kind=recommendation`), S_RegDone |
| 등록 시나리오 ② 영양제 등록 (17 entries) | + S_Search, S_SearchConfirm, S_SearchAdded, S_SelectedEmpty, S_SelectedList, S_SelectedEdit, S_DoseSheet, S_AnalyzeLoading(`kind=supp`), S_Result(`kind=supp`), S_Schedule(`kind=supp`), S_AddNote(`kind=memo`) |

### Scope (명시적 제외 — v1 보류)

- **Tab 3 — 내 정보**: S_MyPage (컴포넌트 미생성 — index.html HTML stub만 존재. IA 모델 Phase 1 작업 #2에서 채울 예정), S_ShareSetup, S_Report
- **Caregiver 모드**: S_CaregiverHome (별도 페르소나, 별도 mode switch)
- **System zone**: S_DosePush (OS push — Tab 1 트리거 row만 명세서 § 4에서 다룸, push 시스템 자체는 차기)
- **Legacy 보존 (명세 제외)**: S_DoseNotice (RegDone으로 교체됨 — 정의·export는 보존, push 미리보기 재사용 후보. IA 모델 § 4 zone 3 표 137행 참조)
- **Onboarding (ENTRY 8 화면)**: S_Splash, S_Welcome, S_Terms, S_ProfileName, S_ProfileChronic, S_PermissionsCare, S_OnboardingLoading, S_OnboardingDone — 1회성 / 차기

### 페르소나 제약 (모든 명세 row 검증 기준)

박정숙 68세 — 만성질환 다제 복용 + 영양제, 스마트폰 초보. 본문 ≥20px, BigButton ≥68px, 한국어 정중·간결, 시간대 컬러 (아침 amber / 점심 mint / 저녁 violet) 고정. `TC.inkFaint`(#6b7280)는 메타데이터 한정.

### 작성 원칙 — 4-layer anchor 피라미드

기능 명세는 단일 산출물이 아니라 **데이터 → 상태 → 계약 → 화면** 순으로 받친다. 각 layer가 다음 layer의 입력.

```
§ 1 ERD (entity·field·관계)         ← PRD §7 Data + 와이어프레임 demo data
   ↓ entity가 무엇이 있는지 결정
§ 2 상태 머신 (lifecycle 전이)       ← index.html flows desc + design/chats microflow
   ↓ entity가 어떤 상태로 변하는지 결정
§ 3 API contract (endpoint × req/res) ← ERD + 상태 전이 트리거
   ↓ 각 화면 액션이 어떤 호출로 이어지는지 결정
§ 4 화면 × 액션 매트릭스             ← 위 3 + IA 모델 + 와이어프레임
```

**Light 형식** (단일 Markdown, 표 위주, mermaid·OpenAPI YAML 사용 안 함). 후속 세션에서 § 2~4 채움.

### 본 문서 진행 상태

| § | 상태 | 비고 |
|---|---|---|
| § 0 Context | ✅ 작성 | scope·제외·페르소나·원칙 |
| § 1 ERD | ✅ 작성 | 10 entity + 8 핵심 결정 + demo 역추적 verification |
| § 2 상태 머신 | ✅ 작성 | 6 lifecycle + 2 시나리오 진입 분기 |
| § 3 API contract | ✅ 작성 | REST endpoints + aggregate read + RN native picker 패턴 + 에러 |
| § 4 화면 × 액션 매트릭스 | ✅ 작성 | Tab 1·Tab 2 + 등록 시나리오 ①·② 전 화면 (variants 별 row) |

---

## § 1 ERD

### 1-A. Entity 목록 (10)

본 범위에서 다루는 데이터는 다음 10 entity로 추적된다. 모든 entity는 `userId` FK를 root tenant로 가진다 (보호자 ACL은 차기 layer).

| Entity | 한 줄 설명 |
|---|---|
| `User` | 시니어 사용자. 기본 시간대(아침/점심/저녁) 시각 보유 |
| `Prescription` | 처방 봉투 (날짜·병원·약국 단위 그룹) |
| `Medication` | 약 항목. 처방약/일반의약품/영양제 통합 (source enum 분기) |
| `Frequency` | 섭취 요일 (매일/평일/격일/주말/custom + selectedDays) |
| `IntakeSchedule` | 복용 슬롯 (Medication × 시간대 × 시각 hh:mm) |
| `IntakeLog` | 복용 기록 (pending → done/skipped/snoozed) |
| `StockSnapshot` | 잔여 점검 기록 (Pattern A — 맞아요 / 다시 셈) |
| `Note` | 사용자 입력 메모 (kind: recommendation 의료인 권고 / memo 개인 메모) |
| `AnalysisResult` | 시스템 분석 결과 (상호작용·중복 — S_AnalyzeLoading→S_Result) |
| `Notification` | 푸시 알림 트리거 (Tab 1 — 복용 시간/잔여 부족/상담 권장) |

> 차기 entity (out of scope): `Caregiver`, `ShareSetting`, `ConsultReport`, `MedicationReference` (공공 의약품·성분 DB 매칭).

### 1-B. 핵심 설계 결정 (8)

| # | 결정 | 근거 |
|---|---|---|
| 1 | **Medication 단일 entity + `source` enum** (`'rx'\|'otc'\|'supp'`) | S_Cabinet의 종류별 그룹은 같은 entity의 filter view. 와이어프레임 `MEDS.prescription / .otc / .supplement` 모두 동일 모양 |
| 2 | **IntakeSchedule 분리** (Medication 1-N) — slot × timeOfDay(hh:mm) × isActive | S_TimeSheet wheel picker(시·분·오전/오후)이 등장하며 시간대 enum만으로 부족. 사용자별로 시각이 달라짐 |
| 3 | **Frequency 별도 entity** — preset(daily/weekday/every_other/weekend/custom) + selectedDays + startDate/endDate | S_DaySheet 격일 복용 케이스. 약 등록 시 약봉지 `durationDays`로 자동 채움 ("매일 (5/2~5/30 · 30일분)") |
| 4 | **Note `kind` enum** — `'recommendation'\|'memo'` 단일 entity | S_AddNote variant 그대로 데이터화. recommendation = 약사/의사/직접 source chips, memo = source=null 본인만 열람 |
| 5 | **AnalysisResult 분리** (Note ≠ AnalysisResult) | Note는 사용자 입력, AnalysisResult는 시스템 도출(약품·성분 DB 조회). 양 시나리오 모두 등장 (`kind='med'` 처방약 간 상호작용 / `kind='supp'` 영양제↔처방약 + 비타민 중복) |
| 6 | **Prescription 1-N Medication** (FK nullable: OTC·Supp는 prescriptionId=null) | S_RxDetail/RxEdit/RxDelete가 Prescription 단위. S_Cabinet에서 처방 supercard로 묶임 |
| 7 | **시간 default 위치** — `User.defaultTimeBuckets` JSON | 사용자 단위 기본 아침/점심/저녁 시각(예: 08:30/12:30/19:00). 등록 시 IntakeSchedule이 복사. S_TimeSheet에서 약별 override |
| 8 | **`userId` root tenant** 모든 entity | 보호자 ACL은 차기에 별도 layer로 추가 (현재 모든 row가 user 1명 소유) |

### 1-C. Entity 상세

#### User

시니어 사용자.

| 필드 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | uuid | PK | |
| name | string | required | "박정숙" |
| chronic | string[] | nullable | 만성질환 list (PRD 페르소나) |
| defaultTimeBuckets | json | required | `{morning: "08:30", noon: "12:30", evening: "19:00"}` (사용자별 기본) |
| createdAt | timestamp | required | |

**관계**: User 1-N Prescription · 1-N Medication · 1-N IntakeLog · 1-N Note · 1-N AnalysisResult · 1-N Notification.

**와이어프레임 매핑**: 페르소나 데이터 (S_Today 헤더 인사 등 어디서든 참조).

---

#### Prescription

처방 봉투. 처방일·병원·약국 단위로 N 개 약을 묶는다.

| 필드 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | uuid | PK | |
| userId | uuid | FK User | |
| prescribedAt | date | required | "5월 10일" → 2026-05-10 |
| clinic | string | required | "사랑내과" |
| pharmacy | string | nullable | "OO약국" (조제) |
| durationDays | int | required | "30일분" → 30. Frequency.endDate 자동 산출 |
| createdAt | timestamp | required | |

**관계**: Prescription 1-N Medication.

**와이어프레임 매핑**: S_Cabinet 처방 supercard, S_RxDetail header, S_RxEdit form, S_Confirm header(추출 결과).

---

#### Medication

약 항목 — 처방약/일반의약품/영양제 통합.

| 필드 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | uuid | PK | |
| userId | uuid | FK User | |
| prescriptionId | uuid? | FK Prescription, nullable | OTC·Supp는 null |
| name | string | required | "아스피린 100mg", "오메가-3 알티지" |
| perDoseAmount | string | required | "1정", "1캡슐", "0.5정" (MVP 자유 텍스트) |
| totalCount | int? | nullable | 영양제 통 단위 (60정), Rx는 null 또는 totalDays 기반 |
| source | enum | `'rx'\|'otc'\|'supp'` | S_Cabinet 종류별 그룹 분기 |
| takingTiming | string? | nullable | "식후 30분", "취침 전" 등 — 처방전 OCR/사용자 |
| brand | string? | nullable | 영양제 브랜드 ("GNM" 등) — supp만 |
| startDate | date | required | |
| endDate | date? | nullable | 처방 만료 / 영양제 보통 null |
| createdAt | timestamp | required | |

**관계**: Prescription 1-N Medication · User 1-N Medication · Medication 1-N IntakeSchedule · Medication 1-1 Frequency · Medication 1-N Note.

**와이어프레임 매핑**:
- S_Cabinet — `MEDS.prescription.groups[].items[]` + `MEDS.otc.items[]` + `MEDS.supplement.items[]`
- S_RxDetail/RxEdit — Prescription joined `rx.items[]`
- S_Confirm — 등록 OCR 결과 (다중 Medication 생성). 표시용 `route` 필드는 client-side derive: `route = perDoseAmount + " · 1일 " + N + "회 · " + Prescription.durationDays + "일분"`. `when` 필드는 `takingTiming`에 1:1 매핑
- S_Today — active medications via IntakeSchedule join

---

#### Frequency

섭취 요일 (격일 복용 등 지원).

| 필드 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | uuid | PK | |
| medicationId | uuid | FK Medication, 1-1 | |
| preset | enum | `'daily'\|'weekday'\|'every_other'\|'weekend'\|'custom'` | S_DaySheet 자주 쓰는 설정 |
| selectedDays | enum[] | `('mon'\|'tue'\|...\|'sun')[]` | preset에서 derive 또는 custom 시 사용자 직접 선택 |
| startDate | date | required | 약 등록 시 처방일 또는 오늘 |
| endDate | date? | nullable | Rx는 prescription.durationDays로 산출, supp는 null |

**관계**: Medication 1-1 Frequency.

**와이어프레임 매핑**:
- S_Schedule `dayLabel` ("매일 (5/2~5/30 · 30일분)" / "매일") → preset + startDate + endDate 표시
- S_DaySheet — preset chips + day chips

---

#### IntakeSchedule

복용 슬롯 (Medication × 시간대 × 시각).

| 필드 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | uuid | PK | |
| medicationId | uuid | FK Medication | |
| slot | enum | `'morning'\|'noon'\|'evening'` | 와이어프레임 아침/점심/저녁 |
| timeOfDay | string | required (hh:mm) | "08:30" — User.defaultTimeBuckets에서 복사, S_TimeSheet에서 override |
| isActive | bool | default true | toggle (S_Schedule) — supp는 일부 OFF |
| doseAmount | string? | nullable | 시간대별 doseAmount override (없으면 Medication.perDoseAmount 사용) |

**관계**: Medication 1-N IntakeSchedule (typically 1~3 row per medication, 시간대 별).

**와이어프레임 매핑**:
- S_Schedule — 3 시간대 카드 (med은 모두 ON, supp는 저녁만 예시)
- S_TimeSheet — `timeOfDay` 편집 (slot 별 wheel picker)
- S_Today `tabData[slot].items[]` — 오늘 시간대 별 표시
- S_RxDetail/RxEdit `rx.items[].time[]` — Pill chips로 표시

---

#### IntakeLog

복용 기록.

| 필드 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | uuid | PK | |
| scheduleId | uuid | FK IntakeSchedule | |
| userId | uuid | FK User | |
| plannedAt | timestamp | required | 예정 시각 (date + IntakeSchedule.timeOfDay) |
| takenAt | timestamp? | nullable | 실제 복용 시각 (status=done일 때만) |
| status | enum | `'pending'\|'done'\|'skipped'\|'snoozed'` | |
| createdAt | timestamp | required | |

**관계**: IntakeSchedule 1-N IntakeLog (날짜별 1 row).

**와이어프레임 매핑**:
- S_Today checked/collapsed prop = 오늘 IntakeLog 상태
- S_Today DoseGroup per-item check circles → IntakeLog status update

---

#### StockSnapshot

잔여 점검 기록 (Pattern A — 맞아요 / 다시 셈).

| 필드 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | uuid | PK | |
| medicationId | uuid | FK Medication | |
| userId | uuid | FK User | |
| remainingValue | int | required | 일치(daysEst) 또는 정(countEst) |
| unit | enum | `'days'\|'pills'` | rx-bag = days, supp = pills |
| method | enum | `'confirmed'\|'recounted'` | 사용자 결정 (Pattern A: 맞아요 / 다시 셈) |
| checkedAt | timestamp | required | |

**관계**: Medication 1-N StockSnapshot (이력).

**와이어프레임 매핑**:
- S_StockCheck `groups[]` (rx-bag daysEst / supp countEst·bottleSize)
- Pattern A 3-state machine (pending → ok / edit) per card

---

#### Note

사용자 입력 메모. kind 분기.

| 필드 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | uuid | PK | |
| medicationId | uuid | FK Medication | |
| userId | uuid | FK User | |
| kind | enum | `'recommendation'\|'memo'` | recommendation = 약 등록 (의료인 권고), memo = 영양제 등록 (개인) |
| content | text | required | 자유 텍스트 (음성 입력 보조 — S_AddNote mic) |
| source | enum? | `'pharmacist'\|'doctor'\|'self'`, recommendation일 때만 | memo는 항상 null |
| createdAt | timestamp | required | |

**관계**: Medication 1-N Note.

**와이어프레임 매핑**:
- S_AddNote `kind="recommendation"` — 약 등록 ("약사님이 뭐라고 하셨나요?", source chips)
- S_AddNote `kind="memo"` — 영양제 등록 ("이 영양제에 대해 메모하실래요?", "본인만 열람" 안내)

---

#### AnalysisResult

시스템 분석 — 상호작용/중복 (Note와 분리).

| 필드 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | uuid | PK | |
| userId | uuid | FK User | |
| triggerMedicationId | uuid | FK Medication | 새로 추가하려는 약/영양제 — 분석 시작점 |
| kind | enum | `'med'\|'supp'` | med = 처방약 간, supp = 영양제↔처방약 |
| level | enum | `'safe'\|'caution'\|'consult'` | "큰 충돌 없음" / "주의" / "상담 권장" |
| pairs | jsonb | nullable | `[{medAId, medBId, note}]` — 상호작용 쌍 (S_Result warning 카드) |
| duplicateNutrients | jsonb | nullable | `[{nutrient: "비타민 D", totalAmount: 1200, unit: "IU", limit: 2000}]` — supp만 (S_Result 비타민 D 카드) |
| summary | text | nullable | 시니어 친화 요약 ("아스피린+오메가-3 함께 드시면 출혈이 늘어날 수 있어요") |
| analyzedAt | timestamp | required | S_AnalyzeLoading 종료 시각 |

**관계**: Medication 1-N AnalysisResult (등록 시마다 새 분석).

**와이어프레임 매핑**:
- S_AnalyzeLoading `kind` (med/supp) — 분석 트리거
- S_Result `kind` (med/supp) — 결과 표시. med = 복용 시점 caution + safe 카드, supp = 출혈 warning + 비타민 D 중복 카드

---

#### Notification

푸시 알림 트리거 (Tab 1).

| 필드 | 타입 | 제약 | 설명 |
|---|---|---|---|
| id | uuid | PK | |
| userId | uuid | FK User | |
| scheduleId | uuid? | FK IntakeSchedule, nullable | intake kind 시 |
| kind | enum | `'intake'\|'stock_low'\|'consult'` | 복용 / 잔여 부족 / 상담 권장 |
| scheduledAt | timestamp | required | 예정 발송 시각 |
| sentAt | timestamp? | nullable | 발송된 시각 |
| createdAt | timestamp | required | |

**관계**: User 1-N Notification.

**와이어프레임 매핑**:
- S_DosePush (System zone — 잠금화면 푸시)
- S_RegDone "이제 약 드실 시간이 되면 알려드릴게요" → IntakeSchedule 등록과 동시에 Notification N개 예약

---

### 1-D. Demo 데이터 역추적 검증

[senior-screens.jsx](docs/components/senior-screens.jsx)의 demo 데이터가 위 entity로 모두 표현 가능한지 확인.

| 와이어프레임 demo | Entity 매핑 |
|---|---|
| S_Today `tabData.evening.items` (`type:'rx-bag', rxDate, clinic, contents[]`) | Medication WHERE prescriptionId IN (그날 처방) AND `IntakeSchedule.slot='evening'` |
| S_Today `tabData.evening.items` (`type:'supp', name, dose`) | Medication (source='supp') JOIN IntakeSchedule WHERE slot='evening' |
| S_Cabinet `MEDS.prescription.groups[]` (date, clinic, items[]) | Prescription JOIN Medication GROUP BY prescription |
| S_Cabinet `MEDS.supplement.items[].dupe` ("비타민B와 중복 가능") | AnalysisResult.duplicateNutrients (또는 derived flag) |
| S_RxDetail `rx.items[].time[]` (`['아침','저녁']`) | IntakeSchedule.slot 집합 (Medication 별) |
| S_StockCheck `groups[]` (rx: daysEst·meds[] / supp: countEst·bottleSize·name) | StockSnapshot (rx는 unit='days', supp는 unit='pills') |
| S_Confirm `route` ("1정 · 1일 2회 · 30일분") + `when` ("식후 30분") | Medication.perDoseAmount + Prescription.durationDays + Medication.takingTiming |
| S_Schedule `dayLabel` ("매일 (5/2~5/30 · 30일분)" / "매일") | Frequency.preset + startDate + endDate 표시 |
| S_Schedule 3 시간대 (timeOfDay, items[]) | IntakeSchedule (slot, timeOfDay, isActive) |
| S_TimeSheet wheel (시·분·오전/오후) | IntakeSchedule.timeOfDay (hh:mm) |
| S_DaySheet preset chips + day chips | Frequency.preset + Frequency.selectedDays |
| S_Result med — "메트포르민 + 식사" caution + "큰 충돌 없음" safe | AnalysisResult.pairs + level='safe' |
| S_Result supp — "아스피린 + 오메가-3" warning + "비타민 D 1,200 IU" 중복 | AnalysisResult.pairs + .duplicateNutrients |
| S_AddNote recommendation — content + source(약사/의사/직접) | Note (kind='recommendation', source) |
| S_AddNote memo — content (출처 chips 없음) | Note (kind='memo', source=null) |
| S_RegDone — 저장 완료 + 오늘 약속 진입 | IntakeSchedule N개 + Frequency 1개 + Note 0~1개 + Notification N개 commit |

### 1-E. 등록 시나리오별 entity 생성 매핑

**시나리오 ① 약 등록 (11 entries)** — Camera→Confirm→Analyze→Result→Schedule→TimeSheet→DaySheet→AddNote→RegDone:

| 단계 | 생성/수정 entity |
|---|---|
| Confirm "3개 모두 추가하기" 탭 | 1 Prescription + N Medication (source='rx') |
| Schedule | N IntakeSchedule (medicationId × slot) + 1 Frequency per medication (preset='daily', startDate=prescribedAt, endDate=prescribedAt + durationDays) |
| TimeSheet | IntakeSchedule.timeOfDay 수정 |
| DaySheet | Frequency.preset / selectedDays 수정 |
| AnalyzeLoading→Result | 1 AnalysisResult (kind='med', triggerMedicationId=신규 약 중 1개 또는 batch) |
| AddNote (선택) | 0~1 Note per medication (kind='recommendation', source) |
| RegDone | Notification N개 예약 (IntakeSchedule × 일정 기간) |

**시나리오 ② 영양제 등록 (17 entries)** — Search→SearchAdded→Selected*→DoseSheet→Analyze→Result→Schedule→TimeSheet→DaySheet→AddNote(memo)→RegDone:

| 단계 | 생성/수정 entity |
|---|---|
| Search→SearchAdded→SelectedList | N Medication (source='supp', prescriptionId=null) |
| SelectedEdit/DoseSheet | Medication.perDoseAmount 수정 |
| Schedule (저녁만 ON 예시) | N IntakeSchedule (isActive 일부) + 1 Frequency (preset='daily') |
| TimeSheet/DaySheet | 위와 동일 |
| AnalyzeLoading→Result | 1 AnalysisResult (kind='supp' — 영양제 ↔ 기존 약, duplicateNutrients 포함) |
| AddNote (선택) | 0~1 Note (kind='memo', source=null) |
| RegDone | Notification N개 |

### 1-F. 핵심 쿼리 가능성 검증

| 화면 / 기능 | 쿼리 (의사 SQL) | 검증 |
|---|---|---|
| Tab 1 S_Today "오늘 복용해야 할 약 by slot" | `SELECT m.*, s.slot, s.timeOfDay FROM IntakeSchedule s JOIN Medication m ON s.medicationId=m.id JOIN Frequency f ON f.medicationId=m.id WHERE m.userId=? AND s.isActive AND today_matches_frequency(f) AND m.startDate<=TODAY AND (m.endDate IS NULL OR m.endDate>=TODAY) ORDER BY s.slot, s.timeOfDay` | 격일 복용은 `today_matches_frequency` 함수에서 `(today - f.startDate) % 2 == 0` 등으로 처리 |
| Tab 1 S_Today 체크 진척도 (저녁 약 X 가지 중 Y완료) | `SELECT count(*) FILTER (WHERE l.status='done') AS done, count(*) AS total FROM IntakeLog l JOIN IntakeSchedule s ON l.scheduleId=s.id WHERE l.plannedAt::date=TODAY AND s.slot='evening'` | |
| Tab 2 S_Cabinet 처방 supercard | `SELECT p.*, count(m.*) FROM Prescription p JOIN Medication m ON m.prescriptionId=p.id WHERE p.userId=? GROUP BY p.id` | |
| Tab 2 S_RxDetail 펼침 | `SELECT m.*, array_agg(s.slot) FROM Medication m JOIN IntakeSchedule s ON s.medicationId=m.id WHERE m.prescriptionId=?` | |
| S_StockCheck 트리거 (잔여 부족) | `SELECT m.*, latest(snapshot) FROM Medication m LEFT JOIN StockSnapshot snap ON snap.medicationId=m.id WHERE m.userId=? AND latest_remaining < threshold` | threshold는 추후 결정 (rx=3일치, supp=10정 등) |
| S_Result 표시 | `SELECT * FROM AnalysisResult WHERE userId=? AND triggerMedicationId IN (방금 등록한 약) ORDER BY analyzedAt DESC LIMIT 1` | |

---

## § 2 상태 머신

### 2-A. 표기 규약

본 절은 **데이터 lifecycle (entity 상태 전이)**과 **flow lifecycle (등록 시나리오 진입 분기)**을 함께 다룬다. 모든 머신은 표 형태:

| from | event / 사용자 액션 | guard (전이 조건) | to | side effect (수반 변화) |

- `from` — 현재 상태
- `event` — 트리거 (사용자 탭 / 시간 도래 / API 응답 / 부모 entity 상태 변화)
- `guard` — 전이가 일어나기 위한 조건 (없으면 `—`)
- `to` — 다음 상태
- `side effect` — 동시에 변화하는 entity / 호출되는 시스템 (push 등)

대상 6 lifecycle + 2 시나리오:

| # | 머신 | 종류 | 위치 |
|---|---|---|---|
| 1 | **Prescription** | entity lifecycle | § 2-B |
| 2 | **Medication** | entity lifecycle | § 2-C |
| 3 | **IntakeLog** | entity lifecycle (날짜별 1 row) | § 2-D |
| 4 | **StockSnapshot 화면 머신** | 화면 내 3-state (Pattern A — 카드 단위) | § 2-E |
| 5 | **AnalysisResult** | entity lifecycle (단발성) | § 2-F |
| 6 | **Notification** | entity lifecycle | § 2-G |
| 7 | **등록 시나리오 ① 약 등록 진입 분기** | flow lifecycle | § 2-H |
| 8 | **등록 시나리오 ② 영양제 등록 진입 분기** | flow lifecycle | § 2-I |

### 2-B. Prescription lifecycle

처방 봉투. S_Confirm "추가하기"로 생성, S_RxEdit으로 수정, S_RxDelete로 삭제.

**States**: `creating` · `saved` · `editing` · `pending_delete` · `deleted`
**Initial**: `creating` (S_Confirm 진입 시)
**Terminal**: `deleted`

| from | event | guard | to | side effect |
|---|---|---|---|---|
| (none) | S_Camera 촬영 → S_Confirm 진입 | OCR 결과 N개 약 | `creating` | 메모리 상 Prescription 임시 객체 + N Medication 임시 객체 |
| `creating` | S_Confirm "N개 모두 추가하기" 탭 | N ≥ 1 | `saved` | DB persist: Prescription + N Medication + 다음 단계로 IntakeSchedule/Frequency 생성 |
| `creating` | TopBar `←` 또는 사용자 이탈 | — | (discarded) | 임시 객체 폐기 |
| `saved` | S_Cabinet supercard 탭 → S_RxDetail | — | `saved` (no transition) | (조회) |
| `saved` | S_RxDetail [수정] 탭 → S_RxEdit 진입 | — | `editing` | S_RxEdit form에 현재 값 prefill |
| `editing` | S_RxEdit "저장" 탭 | 필수 필드 valid | `saved` | DB update: Prescription·Medication·IntakeSchedule·Frequency 다중 update |
| `editing` | S_RxEdit "취소" 탭 / `←` | — | `saved` | 변경 폐기 |
| `saved` | S_RxDetail [삭제] 탭 → S_RxDelete | — | `pending_delete` | dim alert 표시 |
| `pending_delete` | S_RxDelete [삭제] 탭 | 사용자 확인 | `deleted` | DB cascade delete: Prescription + 모든 Medication·IntakeSchedule·Frequency·Note. Notification N개 cancel. **되돌릴 수 없음** |
| `pending_delete` | S_RxDelete [취소] 탭 / dim 영역 탭 | — | `saved` | alert 닫기 |

**부가**: `creating` 상태 중에 사용자가 약 list를 일부 수정 가능 (S_Confirm 터치 수정). 이는 임시 Medication 임시 객체에 반영.

### 2-C. Medication lifecycle

약 항목. Prescription에 종속(rx) 또는 독립(otc/supp).

**States**: `creating` · `active` · `editing` · `expired` · `deleted`
**Initial**: `creating` (등록 flow 중)
**Terminal**: `deleted`

| from | event | guard | to | side effect |
|---|---|---|---|---|
| (none) | 등록 flow 진행 (Confirm 또는 SelectedList 추가) | — | `creating` | 임시 Medication 객체 |
| `creating` | RegDone 도달 (저장 완료) | parent saved | `active` | DB persist + Frequency·IntakeSchedule 동시 생성 |
| `active` | S_RxEdit 진입 (rx) 또는 cabinet 탭 편집 (supp) | — | `editing` | form prefill |
| `editing` | 저장 | valid | `active` | update |
| `editing` | 취소 | — | `active` | discard |
| `active` | (auto) endDate 경과 | endDate not null AND today > endDate | `expired` | IntakeSchedule.isActive = false (cascading), Notification 자동 cancel |
| `active` | 단건 삭제 (rx 외 OTC·supp만 가능) | source != 'rx' OR (parent prescription 같이 삭제) | `deleted` | DB delete + IntakeSchedule·Frequency cascade |
| `expired` | 사용자 재처방 등록 시 새 Medication 생성 | — | (new instance) | 별개 row |

**Note**: rx Medication은 단독 삭제 불가 — Prescription 단위로만 삭제(`pending_delete`). otc/supp는 단독 삭제 가능 (S_Cabinet에서 swipe 또는 cabinet 탭의 편집 모드).

### 2-D. IntakeLog lifecycle (날짜별 1 row, IntakeSchedule 별)

복용 기록. IntakeSchedule × 날짜 1 row. 매일 자정 또는 첫 진입 시 lazy 생성.

**States**: `pending` · `done` · `skipped` · `snoozed` · `missed`
**Initial**: `pending` (해당 날짜 진입 시 생성)
**Terminal**: `done` · `skipped` · `missed` (terminal — 다음 날 새 row)

| from | event | guard | to | side effect |
|---|---|---|---|---|
| (none) | 날짜 진입 (S_Today 첫 로드 또는 cron) | IntakeSchedule.isActive AND Frequency matches today | `pending` | row 생성, Notification scheduledAt 등록 |
| `pending` | scheduledAt 도래 | — | `pending` (no transition) | Notification 발송 (push) |
| `pending` | S_Today DoseGroup 체크 원 탭 | — | `done` | takenAt = now |
| `pending` | S_Today "건너뛰기" (또는 push action) | — | `skipped` | takenAt = null |
| `pending` | "나중에 다시 알려줘" (snooze) | — | `snoozed` | nextRemindAt = now + N분 |
| `snoozed` | nextRemindAt 도래 | — | `pending` | Notification 재발송 |
| `snoozed` | 체크 탭 | — | `done` | takenAt = now |
| `pending` | (auto) 해당 시간대 + 4시간 경과 | sentAt 후 4h | `missed` | `caregiverShare`(차기) 트리거 |
| `done` | (사용자 실수 시 되돌리기) | UI 미정 | `pending` | takenAt = null |

**부가**: S_Today `checked` prop = 그날의 모든 IntakeLog가 `done`. `collapsed` prop = 모두 done 상태에서 사용자가 탭해 접은 view 상태. Today entity가 아니라 query 결과.

### 2-E. StockSnapshot 화면 머신 (Pattern A — 카드 단위 3-state)

S_StockCheck의 각 medication 카드가 가지는 임시 UI 상태. 페이지 단위가 아니라 **카드 단위**로 독립적.

**States**: `pending` · `ok` · `edit`
**Initial**: `pending` (페이지 진입 시 모든 카드)
**Terminal**: 없음 — 페이지 이탈 시 ok/edit 상태가 StockSnapshot DB row로 commit

| from | event | guard | to | side effect |
|---|---|---|---|---|
| (none) | S_StockCheck 진입 | trigger 발생 (특정 요일 거름 등) | `pending` | UI 상태 default |
| `pending` | "맞아요" 탭 | — | `ok` | 카드 색·체크 표시. method='confirmed' |
| `pending` | "다시 셀게요" 탭 | — | `edit` | 입력 필드 (-/+ 또는 keypad) 표시 |
| `edit` | 값 입력 + 확정 | value > 0 | `ok` | remainingValue = 입력값. method='recounted' |
| `edit` | 취소 | — | `pending` | 입력 폐기 |
| `ok` (어느 한 카드라도) | "완료" BigButton 탭 | 모든 카드가 `ok` (또는 일부 ok이면 일부 commit) | (페이지 이탈) | StockSnapshot DB row N개 INSERT (ok 상태인 카드만) |

**Note**: Pattern A 핵심은 1-tap 기본값(맞아요)과 2-tap 정밀 경로(다시 셈) 분리. 시니어 인지부담 최소화.

### 2-F. AnalysisResult lifecycle (단발성)

S_AnalyzeLoading 진입 시 1회 분석. 결과는 1 row로 영구 저장(이력).

**States**: `computing` · `ready` · `failed`
**Initial**: `computing`
**Terminal**: `ready` · `failed`

| from | event | guard | to | side effect |
|---|---|---|---|---|
| (none) | S_AnalyzeLoading 진입 (등록 flow 중) | trigger Medication(s) 결정됨 | `computing` | 백엔드 분석 호출 (의약품·성분 DB 조회) |
| `computing` | 백엔드 응답 success | — | `ready` | level/pairs/duplicateNutrients/summary fill, S_Result로 자동 전이 |
| `computing` | 백엔드 응답 fail / timeout | — | `failed` | 에러 표시 + 재시도 옵션 (UI는 차기) |
| `failed` | 재시도 탭 | — | `computing` | 새 분석 호출 |
| `ready` | 사용자 S_Result에서 "약통에 추가" 탭 | level != 'consult' OR 사용자 수락 | (Medication active로) | 부모 Medication 활성화 진행. AnalysisResult는 historical row로 잔존 |

**부가**: AnalysisResult는 **단발성** (transient computation). Medication 1-N AnalysisResult 관계로 이력 보존, 최신 row만 S_Result에 표시.

### 2-G. Notification lifecycle

푸시 알림 트리거. IntakeSchedule 등록과 동시에 N개 예약, 사용자 액션 또는 시간 도래로 발송/취소.

**States**: `scheduled` · `sent` · `cancelled`
**Initial**: `scheduled`
**Terminal**: `sent` · `cancelled`

| from | event | guard | to | side effect |
|---|---|---|---|---|
| (none) | RegDone 도달 / IntakeSchedule 변경 | scheduledAt 미래 | `scheduled` | OS push 예약 (Notifee/expo-notifications) |
| `scheduled` | scheduledAt 도래 | — | `sent` | OS 발송. S_DosePush(잠금화면) 표시 |
| `scheduled` | Medication `expired`/`deleted` (cascade) | — | `cancelled` | OS push 취소 |
| `scheduled` | IntakeSchedule.isActive = false | — | `cancelled` | OS push 취소 |
| `scheduled` | IntakeSchedule.timeOfDay 변경 | — | `cancelled` (이전), 새 `scheduled` (대체) | 재예약 |
| `sent` | 사용자 push 탭 | — | `sent` (terminal) | App deep-link → SeniorTabs/Today |

### 2-H. 등록 시나리오 ① 약 등록 — flow lifecycle (11 화면)

linear 시퀀스. 사용자 액션으로 다음 화면 진입.

**States** (= 화면 ID): `add_type` → `add_choice` → `camera` → `confirm` → `analyze_loading_med` → `result_med` → `schedule_med` → (옵션 시트: `time_sheet` / `day_sheet`) → `add_note_recommendation` → `reg_done`
**Initial**: `add_type` (Tab 2 + FAB / S_Cabinet `+` 탭)
**Terminal**: `reg_done` (또는 사용자 이탈로 모든 임시 객체 폐기)

| from | event | guard | to | side effect |
|---|---|---|---|---|
| (외부) Tab 2 진입 + 등록 트리거 | "약 추가" 탭 | — | `add_type` | S_AddType modal 표시 |
| `add_type` | "복용의약품" 선택 | — | `add_choice` | (영양제 선택 시 시나리오 ②) |
| `add_choice` | "사진으로 추가" 선택 | — | `camera` | 카메라 권한 확인 |
| `add_choice` | "이름으로 찾기" 선택 | — | (시나리오 ② Search로 분기 — 약 검색은 supp DB 공유) | |
| `add_choice` | "말로 알려주기" / 직접 입력 | — | `confirm` | S_Confirm을 빈 폼으로 재사용 |
| `camera` | 촬영 + OCR 응답 | OCR 약 N≥1 | `confirm` | 임시 Prescription + N Medication 객체 |
| `confirm` | "N개 모두 추가하기" 탭 | N≥1 | `analyze_loading_med` | Prescription + Medication persist (Prescription `creating` → `saved`), AnalysisResult kind='med' 시작 |
| `analyze_loading_med` | 분석 응답 success | — | `result_med` | AnalysisResult `ready` |
| `analyze_loading_med` | 분석 응답 fail | — | (재시도 / 우회 — UI 차기) | |
| `result_med` | "약통에 추가" 탭 | level != consult OR 수락 | `schedule_med` | Medication `active` |
| `result_med` | "나중에 결정" | — | `schedule_med` | 동일 (시니어 wireframe — 흐름 막지 않음) |
| `schedule_med` | 시간대 카드 탭 | — | `time_sheet` (sheet over) | S_TimeSheet 표시, 슬롯 컨텍스트 전달 |
| `time_sheet` | "확인" 탭 | timeOfDay valid | `schedule_med` | IntakeSchedule.timeOfDay update |
| `time_sheet` | "취소" / 백드롭 탭 | — | `schedule_med` | 변경 폐기 |
| `schedule_med` | "섭취 요일" row 탭 | — | `day_sheet` | S_DaySheet 표시 |
| `day_sheet` | "확인" 탭 | preset 또는 selectedDays valid | `schedule_med` | Frequency.preset/selectedDays update |
| `day_sheet` | "취소" / 백드롭 탭 | — | `schedule_med` | 변경 폐기 |
| `schedule_med` | "완료" 탭 | — | `add_note_recommendation` | IntakeSchedule N개 + Frequency persist |
| `add_note_recommendation` | "저장" 탭 (또는 비워두고 진행) | — | `reg_done` | (입력 시) Note kind='recommendation' persist |
| `reg_done` | "오늘 약속 보러 가기" 탭 | — | (Tab 1 — S_Today) | 모든 commit 확정 + Notification N개 schedule |
| `reg_done` | "약통 둘러보기" 탭 | — | (Tab 2 — S_Cabinet) | |
| (any) | TopBar `←` 반복 / 사용자 이탈 | — | (이전 화면 또는 종료) | 단계별 — `confirm` 이전은 임시 객체 폐기, `confirm` 이후는 Prescription saved 상태 잔존 |

### 2-I. 등록 시나리오 ② 영양제 등록 — flow lifecycle (17 화면)

`add_type` → `add_choice` → `camera` (영양제 라벨) → `search` → `search_confirm` → `search_added` → `selected_empty` → `selected_list` → `selected_edit` → `dose_sheet` → `analyze_loading_supp` → `result_supp` → `schedule_supp` → (`time_sheet` / `day_sheet`) → `add_note_memo` → `reg_done`

기본 분기 흐름은 ①과 같고, 다음 차이만 명시:

| from | event | guard | to | side effect |
|---|---|---|---|---|
| `add_type` | "영양제" 선택 | — | `add_choice` | (med 분기는 시나리오 ①) |
| `add_choice` | "이름으로 찾기" 선택 (영양제는 검색이 주 진입) | — | `search` | |
| `search` | 검색어 입력 + 결과 탭 | — | `search_confirm` | "찾으시는 영양제가 맞나요?" modal |
| `search_confirm` | 확인 | — | `search_added` | Medication 임시 객체 (source='supp') 추가, `selectedList` 카운트 +1 |
| `search_confirm` | 다른 결과 | — | `search` | |
| `search_added` | 우하단 FAB "다 골랐어요" 탭 | count ≥ 1 | `selected_list` | (count = 0이면 `selected_empty`) |
| `selected_list` | 카드 탭 | — | `selected_edit` | toggle ON, dose pill 표시 |
| `selected_edit` | dose pill 탭 | — | `dose_sheet` (sheet over) | S_DoseSheet 표시 (0.5/1/1.5) |
| `dose_sheet` | 값 선택 + "확인" | — | `selected_edit` | Medication.perDoseAmount update |
| `selected_edit` | "다음" 탭 | 선택된 영양제 ≥ 1 | `analyze_loading_supp` | Medication N개 persist (source='supp', prescriptionId=null) |
| `analyze_loading_supp` | 분석 응답 success | — | `result_supp` | AnalysisResult kind='supp' (영양제↔처방약 + 비타민 중복) |
| `result_supp` | "약통에 추가" 또는 "나중에 결정" | — | `schedule_supp` | |
| `schedule_supp` ~ `add_note_memo` | (시나리오 ①과 동일 sub-flow — kind 분기만 다름) | — | — | Note kind='memo' (출처 chips 없음) |
| `add_note_memo` → `reg_done` | "저장" | — | `reg_done` | 시나리오 ①과 동일 |

**공통 분기**: 양 시나리오 모두 `add_type` → `add_choice` → `camera`/`search`/직접 진입은 단일 path. `analyze_loading` 이후 흐름(Result → Schedule → AddNote → RegDone)도 동일 모양 (kind prop 차이만).

---

## § 3 API contract

### 3-A. 공통 규약

**Auth**: Bearer token (`Authorization: Bearer <jwt>`). 모든 endpoint는 userId scope (token에서 추출). `userId`를 Request body에 명시할 필요 없음 — 서버가 token에서 주입.

**Base URL**: `https://api.yaksok.app/v1` (예시 — 실제 환경 별도 결정).

**Content-Type**: `application/json`.

**Date/Time 표현**:
- `date`: ISO-8601 date — `"2026-05-10"`
- `timestamp`: ISO-8601 datetime UTC — `"2026-05-10T22:00:00Z"`
- `timeOfDay`: 24h hh:mm — `"08:30"`

**ID 형식**: 모두 uuid (`"a1b2c3d4-..."`).

**Pagination** (목록 endpoint): cursor 기반 — query param `?cursor=<opaque>&limit=20`. 응답에 `nextCursor` 포함.

**Idempotency**: 사용자 의도가 동일한 POST(예: S_Confirm "추가하기" 더블탭)는 헤더 `Idempotency-Key: <uuid>` 권장.

### 3-B. Aggregate / Read 최적화 endpoint

시니어 모바일 — round-trip 최소화. 첫 로드 시 단일 GET으로 화면 그릴 데이터 모두 받기.

| Method | Path | 화면 | 응답 요약 |
|---|---|---|---|
| GET | `/me` | (모든 화면 — 사용자 컨텍스트) | User + defaultTimeBuckets |
| GET | `/today?date=2026-05-10` | S_Today (Tab 1 진입) | { user, slotItems[][], todaySummary } — slotItems: 시간대별 medication+intakeLog joined list |
| GET | `/today/calendar?from=...&to=...` | S_Today 헤더 월간 expand | 각 날짜별 status('done'\|'partial'\|'missed'\|'future') |
| GET | `/cabinet` | S_Cabinet (Tab 2 진입) | { prescriptions: [...with medications], otc: [...], supplements: [...] } |
| GET | `/stock/check-needed` | S_StockCheck 트리거 판정 | { groups: [{type:'rx', date, clinic, daysEst, meds[]}, {type:'supp', name, countEst, bottleSize}] } |

**`GET /today` 응답 예시**:
```
{
  "user": { "id": "...", "name": "박정숙", "defaultTimeBuckets": {...} },
  "date": "2026-05-10",
  "slotItems": {
    "morning": [
      { "type":"rx-bag", "prescriptionId":"...", "rxDate":"2026-05-10", "clinic":"사랑내과",
        "contents":[{"medicationId":"...","name":"메트포르민","perDoseAmount":"1정"}],
        "intakeLogs": [{"scheduleId":"...","status":"pending","plannedAt":"2026-05-10T23:30:00Z"}] }
    ],
    "noon": [...],
    "evening": [
      { "type":"rx-bag", ... },
      { "type":"supp", "medicationId":"...","name":"오메가-3","perDoseAmount":"1캡슐",
        "intakeLog":{"status":"pending"} }
    ]
  },
  "todaySummary": { "total": 5, "done": 3, "remaining": 2, "isAllDone": false }
}
```

### 3-C. Prescription endpoints (S_Confirm / S_RxDetail / S_RxEdit / S_RxDelete)

| Method | Path | 화면 | 머신 트리거 |
|---|---|---|---|
| POST | `/prescriptions` | S_Confirm "N개 모두 추가하기" | Prescription `creating` → `saved`, Medication N×`creating` → `active`, IntakeSchedule/Frequency 동시 생성 |
| GET | `/prescriptions/{id}` | S_RxDetail 진입 | (조회) |
| PATCH | `/prescriptions/{id}` | S_RxEdit "저장" | Prescription `editing` → `saved` (+ 종속 Medication·Schedule 다중 update) |
| DELETE | `/prescriptions/{id}` | S_RxDelete "삭제" | Prescription `pending_delete` → `deleted`, cascade |

**POST `/prescriptions` Request**:
```
{
  "idempotencyKey": "...",     // 권장
  "prescribedAt": "2026-05-10",
  "clinic": "사랑내과",
  "pharmacy": "OO약국",          // optional
  "durationDays": 30,
  "medications": [
    {
      "name": "메트포르민",
      "perDoseAmount": "1정",
      "takingTiming": "식후 30분",
      "totalDays": 30,
      "schedules": [
        { "slot": "morning", "timeOfDay": "08:30" },
        { "slot": "evening", "timeOfDay": "19:00" }
      ],
      "frequency": { "preset": "daily", "selectedDays": ["mon",...,"sun"] }
    },
    ...
  ]
}
```

**POST 201 Response**: `{ "prescription": {...}, "medications": [...], "schedules": [...], "frequencies": [...] }`. RN은 이 응답으로 화면 상태 갱신.

**PATCH `/prescriptions/{id}` Request**: 부분 update — `clinic`, `prescribedAt`, `medications` 재정의 등. 약 list가 변경되면 추가/삭제는 별도 child endpoint로 (아래 § 3-D 참조) 또는 본 endpoint에서 medications 전체 교체로 처리. **권장**: 후자 (단일 트랜잭션, 시니어 wireframe 단순함 우선).

**DELETE `/prescriptions/{id}` Request**: body 없음. **하드 삭제** — 응답 `204 No Content`. 클라이언트는 OS push N개도 cancel 호출 (또는 서버가 DB cascade 시 알림 cancel job 트리거).

**Errors**:
- `400 PRESCRIPTION_VALIDATION` — durationDays ≤ 0, medications 비어있음, schedules slot 중복 등
- `404 NOT_FOUND` — id 없음 (또는 다른 사용자 소유)
- `409 IDEMPOTENCY_CONFLICT` — 동일 key 다른 payload

### 3-D. Medication endpoints

처방약(rx) 단독 CRUD는 Prescription endpoint로 처리 (cascade). 영양제(supp)·일반의약품(otc)는 단독 CRUD.

| Method | Path | 화면 | 머신 트리거 |
|---|---|---|---|
| POST | `/medications` | S_SearchAdded → SelectedList 진행 시 batch (영양제) / 직접 입력 | Medication N×`creating` → `active` (source='supp'\|'otc') |
| GET | `/medications/{id}` | (S_RxEdit 내부) | (조회) |
| PATCH | `/medications/{id}` | S_SelectedEdit dose 수정, S_RxEdit 약별 수정 | Medication `editing` → `active` |
| DELETE | `/medications/{id}` | S_Cabinet supp/otc 삭제 (UI는 차기 — swipe or 편집 모드) | Medication `active` → `deleted`. **rx는 거부** (Prescription 단위로만) |

**POST `/medications`** body 예시 (영양제 batch):
```
{
  "items": [
    { "source": "supp", "name": "오메가-3 알티지", "brand": "GNM",
      "perDoseAmount": "1캡슐", "totalCount": 60,
      "schedules": [{"slot":"evening","timeOfDay":"19:00"}],
      "frequency": {"preset":"daily","selectedDays":[...]} },
    ...
  ]
}
```

**Errors**: `403 RX_DELETE_FORBIDDEN` — rx Medication 단독 삭제 시도.

### 3-E. IntakeSchedule + Frequency endpoints

부모 Medication에 종속. S_Schedule "완료" 시 batch update, S_TimeSheet/S_DaySheet는 단일 row update.

| Method | Path | 화면 | 머신 트리거 |
|---|---|---|---|
| PATCH | `/medications/{id}/schedules` | S_Schedule "완료" | IntakeSchedule N row upsert (slot × timeOfDay × isActive). Notification 재예약 |
| PATCH | `/schedules/{id}` | S_TimeSheet "확인" | 단일 row — timeOfDay 변경. Notification cancel + new schedule |
| PATCH | `/medications/{id}/frequency` | S_DaySheet "확인" | Frequency 1 row update — preset/selectedDays/startDate/endDate |

**PATCH `/medications/{id}/schedules` Request**:
```
{
  "schedules": [
    {"slot":"morning", "timeOfDay":"08:30", "isActive":true},
    {"slot":"noon",    "timeOfDay":"12:30", "isActive":true},
    {"slot":"evening", "timeOfDay":"19:00", "isActive":true}
  ]
}
```

**PATCH `/schedules/{id}` Request**: `{"timeOfDay":"09:00"}` (또는 `{"isActive":false}`).

**PATCH `/medications/{id}/frequency` Request**: `{"preset":"every_other","selectedDays":["mon","wed","fri","sun"]}`.

### 3-F. IntakeLog endpoints (Tab 1 체크)

| Method | Path | 화면 | 머신 트리거 |
|---|---|---|---|
| POST | `/intake-logs` | S_Today DoseGroup 체크 원 탭 | IntakeLog `pending` → `done` (또는 lazy 생성 후 done) |
| POST | `/intake-logs/skip` | S_Today "건너뛰기" | `pending` → `skipped` |
| POST | `/intake-logs/snooze` | "나중에 다시" | `pending` → `snoozed` (+ nextRemindAt) |
| PATCH | `/intake-logs/{id}` | (실수 되돌리기 — UI 차기) | `done` → `pending` 등 |
| GET | `/intake-logs?from=...&to=...` | S_Today 헤더 월간 expand 그리드 | (집계) — § 3-B `/today/calendar` 참조 |

**POST `/intake-logs` Request**:
```
{
  "scheduleId": "...",
  "plannedAt": "2026-05-10T19:00:00+09:00",  // 클라이언트 로컬 시간 → 서버 변환
  "status": "done"
}
```

서버는 `(scheduleId, plannedAt::date)` 유일성 보장 (해당 날짜 1 row only). 동일 키로 재 POST 시 `200 OK` (idempotent).

### 3-G. StockSnapshot endpoints

S_StockCheck "완료" 탭 시 ok 상태 카드 batch.

| Method | Path | 화면 | 머신 트리거 |
|---|---|---|---|
| POST | `/stock-snapshots/batch` | S_StockCheck "완료" | StockSnapshot N row INSERT. 카드 `ok` 상태만 (edit 미확정·pending은 제외) |

**Request**:
```
{
  "snapshots": [
    {"medicationId":"...", "remainingValue":4,  "unit":"days",  "method":"confirmed"},
    {"medicationId":"...", "remainingValue":25, "unit":"pills", "method":"recounted"}
  ]
}
```

### 3-H. Note endpoints

S_AddNote — 약 등록 시 recommendation, 영양제 등록 시 memo.

| Method | Path | 화면 | 머신 트리거 |
|---|---|---|---|
| POST | `/medications/{id}/notes` | S_AddNote "저장" | Note 생성 (kind+source+content) |
| GET | `/medications/{id}/notes` | (S_RxDetail 확장 — UI 차기) | (조회 list) |
| PATCH | `/notes/{id}` | (편집 — UI 차기) | content 수정 |
| DELETE | `/notes/{id}` | (삭제 — UI 차기) | 단순 삭제 |

**POST `/medications/{id}/notes` Request**:
```
{
  "kind": "recommendation",  // 또는 "memo"
  "content": "어지러우면 약사에게 알려달라고 하셨어요",
  "source": "pharmacist"      // recommendation일 때만 필수, memo는 null
}
```

**Validation**: `kind=memo` AND `source != null` → `400 NOTE_KIND_SOURCE_MISMATCH`.

### 3-I. AnalysisResult endpoints (S_AnalyzeLoading → S_Result)

| Method | Path | 화면 | 머신 트리거 |
|---|---|---|---|
| POST | `/analyses` | S_AnalyzeLoading 진입 | AnalysisResult `computing` 생성 + 백엔드 분석 시작 |
| GET | `/analyses/{id}` | S_AnalyzeLoading polling | `computing` 상태 확인. `ready` 시 결과 fill |

**POST `/analyses` Request**:
```
{
  "kind": "med",                   // "med" | "supp"
  "triggerMedicationIds": ["...", "...", "..."]  // 신규 약 또는 영양제
}
```

**POST 202 Accepted**: `{"analysisId":"...", "status":"computing"}`. 클라이언트는 즉시 S_AnalyzeLoading 표시.

**GET `/analyses/{id}` 200 Response (ready)**:
```
{
  "id": "...",
  "status": "ready",
  "kind": "supp",
  "level": "consult",
  "pairs": [
    {"medAId":"...","medAName":"아스피린","medBId":"...","medBName":"오메가-3",
     "note":"함께 드시면 출혈이 늘어날 수 있어요. 약사님과 한 번 상의해 주세요."}
  ],
  "duplicateNutrients": [
    {"nutrient":"비타민 D","totalAmount":1200,"unit":"IU","limit":2000,"sources":["비타민B 콤플렉스","비타민D 1000IU"]}
  ],
  "summary": "아스피린과 오메가-3를 함께 드시면 출혈 위험이 있어요.",
  "analyzedAt": "..."
}
```

**Polling vs WebSocket**: MVP는 polling (3초 간격). 분석 평균 < 5초면 충분. 차기에 WebSocket으로 push.

**Errors**: `503 ANALYSIS_TIMEOUT` (re-try 가능), `424 REFERENCE_DB_UNAVAILABLE` (의약품 DB 불가).

### 3-J. Notification endpoints

대부분 자동 (서버 cron + IntakeSchedule 변경 hook). 사용자 직접 호출은 설정 정도.

| Method | Path | 화면 | 머신 트리거 |
|---|---|---|---|
| POST | `/notifications/register-token` | (Onboarding 권한 — 본 명세 scope 외) | OS push token 등록 |
| PATCH | `/notifications/preferences` | (Tab 3 알림 설정 — scope 외) | 알림 종류별 on/off |
| GET | `/notifications/upcoming` | S_RegDone 안내 텍스트 검증용 | 향후 N개 예정 알림 list (debugging) |

내부 자동 변환 규칙:
- IntakeSchedule 생성/변경 → Notification scheduled (매일 또는 frequency 매칭일)
- Medication endDate 도래 → Notification cancel
- Prescription delete → 종속 Notification 일괄 cancel

### 3-K. RN 클라이언트 패턴 (native picker / 권한)

본 wireframe의 일부 sheet은 RN 단계에서 native UI로 대체될 수 있음:

| Wireframe sheet | RN 권장 구현 | 이유 |
|---|---|---|
| `S_TimeSheet` (시·분·오전/오후 wheel) | iOS: `DateTimePickerIOS` (display:'spinner') / Android: `TimePickerAndroid` | 플랫폼 native가 시니어에게 익숙, 접근성 자동 |
| `S_DaySheet` (요일 chips + presets) | 직접 구현 (RN 컴포넌트) — native 동등물 없음 | 와이어프레임 대로 구현 |
| `S_DoseSheet` (0.5/1/1.5 picker) | 직접 구현 — chip group | |
| `S_RxDetail` / `S_RxEdit` (커스텀 sheet) | iOS: `<Modal presentationStyle="pageSheet">` / Android: `BottomSheetDialog` | 시각적 일관성 + native gesture (swipe-to-dismiss) |
| `S_RxDelete` (centered alert) | iOS/Android `Alert.alert` 또는 커스텀 destructive modal | 시스템 alert이 시니어 기대에 부합 |

**필요 권한**:
- 카메라 (S_Camera) — `expo-camera` 또는 `react-native-vision-camera`
- 마이크 (S_AddNote 음성 입력) — STT 서비스 + 권한
- Notification — 등록 시 1회 (OS 다이얼로그)
- 사진 라이브러리 — 갤러리 선택 fallback (와이어프레임 미반영, 차기)

### 3-L. 공통 에러 응답

모든 endpoint는 다음 형식:
```
{
  "error": {
    "code": "PRESCRIPTION_VALIDATION",
    "message": "약 list가 비어 있어요.",       // 시니어 친화 한글 (UI 그대로 노출 가능)
    "details": {...}                          // optional 디버그 정보
  }
}
```

**HTTP 상태별 분류**:

| HTTP | 의미 | 클라이언트 처리 |
|---|---|---|
| `400` | Validation 실패 | inline 에러 노출 |
| `401` | 토큰 만료/무효 | 재로그인 |
| `403` | 권한 없음 (다른 사용자 데이터 / rx 단독 삭제 등) | "변경할 수 없어요" 토스트 |
| `404` | 자원 없음 | "찾을 수 없어요" + back |
| `409` | 충돌 (idempotency / 중복) | retry-with-key |
| `424` | 외부 의존(의약품 DB 등) | "잠시 후 다시 시도해 주세요" |
| `429` | rate limit | exponential backoff |
| `5xx` | 서버 오류 | 재시도 1회 + 실패 시 "잠시 후 다시 시도해 주세요" |

**시니어 카피 원칙**: error.message는 곧장 화면에 노출 가능한 정중·간결 한글. 기술 스택 노출 금지 ("OCR 실패" 대신 "사진을 다시 찍어 주세요" 등).

---

## § 4 화면 × 액션 매트릭스

### 4-A. 표기 규약

각 화면 sub-section 구성:
- **진입**: 어떤 라우트·이벤트로 도달하는지
- **표시 데이터**: § 1 entity의 어떤 field가 어디에 매핑되는지 + § 3 API 입력 source
- **props (있을 때)**: variant 분기 — variant 별로 표시·액션이 달라지는 점만 명시
- **액션 표**: 사용자 액션마다 (트리거 / 다음 화면·상태 / API / 에러) 한 row

**약자**: 
- ⬆️ = 시트 (sheet) over 부모 화면 (backdrop)
- 🔲 = 모달 (centered alert / dim 0.5)
- → = 화면 전이 (스택 push)
- ← = 백 (스택 pop, 상위 라우트 복귀)
- (commit) = 부모 entity DB persist 트리거

본 절은 `Tab 1 (4-B)` · `Tab 2 (4-C)` · `등록 시나리오 ① (4-D)` · `등록 시나리오 ② — deltas (4-E)` 순. 시나리오 ②는 ①과 공유 화면(AddType, AddChoice, Camera, AnalyzeLoading variant, Result variant, Schedule variant, TimeSheet, DaySheet, AddNote variant, RegDone)은 § 4-D 참조하고 차이점만 명시.

---

### 4-B. Tab 1 (오늘)

#### S_Today

**진입**: TabBar `오늘` 탭 / 앱 cold start / S_DosePush deep-link / 등록 RegDone "오늘 약속 보러 가기".
**표시 데이터** (from `GET /today`):
- 헤더: 오늘 날짜 + 주간 streak strip(`/today/calendar` 7일치) + 월간 expand
- 시간대 별 DoseGroup 카드 — `slotItems[slot][]` (rx-bag 또는 supp 단위)
- 각 약 row: name, perDoseAmount, IntakeLog.status (체크 원 ON/OFF)
- 하단 진척도: `todaySummary.done / .total`

**props**:

| variant | 진입 조건 | 차이점 |
|---|---|---|
| (default) | 활성 medication ≥ 1 + 진행 중 | 시간대 카드 normal 표시 |
| `empty=true` | active medication 0개 | 상단 hero "약을 등록해 시작해 보세요" + CTA "약 추가하기" → S_AddType. 시간대 카드 빈 상태 |
| `checked=true` | 오늘 모든 IntakeLog `done` | 저녁 카드 breathe(safe tone) — 완료 강조 |
| `checked=true, collapsed=true` | checked 상태 + 사용자가 카드 접음 | 시간대 카드 헤더만 표시, "상세 보기" 탭으로 펼침 |

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 시간대별 약 체크 | DoseGroup 원 탭 | in-place check 표시 (낙관적) | `POST /intake-logs` { scheduleId, plannedAt, status:'done' } | 5xx → toast "잠시 후 다시 시도해 주세요" + 원 rollback |
| 약 체크 해제 (실수 되돌리기) | 체크된 원 long press 또는 동일 row 탭 | undo (UI 차기) | `PATCH /intake-logs/{id}` { status:'pending' } | 동일 |
| 주간 streak 탭 | 헤더 weekStrip 탭 | renderMonthGrid expand (in-page) | `GET /today/calendar?from=&to=` | inline 빈 상태 |
| 월간 그리드 → 특정 일 탭 | 그리드 cell 탭 | (조회 — 차기) | — | — |
| 약 추가 (empty variant CTA) | "약 추가하기" 탭 | → S_AddType | — | — |
| 시간대 카드 접기/펼치기 (checked variant) | 카드 우상단 ⌃ 탭 | collapsed prop toggle | — | — |
| Tab 이동 | TabBar `내 약` / `내 정보` | → S_Cabinet / S_MyPage | — | — |

**Validation/Edge cases**:
- 빈 상태 + empty CTA가 활성 (Frequency 매칭일이 아닌 날도 빈 카드 처리)
- 격일 복용 약은 오늘 미매칭 시 `slotItems`에 미포함 (서버 필터링)
- IntakeLog는 lazy 생성 — `POST /intake-logs` 시 서버가 row 없으면 생성, 있으면 update (idempotent)

#### S_StockCheck

**진입**: 주간 트리거(특정 요일 미복용 등 — 차기 정의) / Tab 1 알림 → S_StockCheck deep-link.
**표시 데이터** (from `GET /stock/check-needed`):
- 헤더: "지금까지 먹은 약 중간 점검할게요"
- 그룹 카드 N개 — 각 카드: type(rx/supp), 라벨(처방약/영양제), name 또는 rx 그룹 정보, daysEst/countEst

**카드 단위 머신** (Pattern A — § 2-E 참조): pending → ok / edit.

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| "맞아요" 탭 | 카드 [맞아요] 버튼 | 카드 `pending` → `ok` (UI만, commit 보류) | — | — |
| "다시 셀게요" 탭 | 카드 [다시 셀게요] 버튼 | 카드 `pending` → `edit` (입력 필드 표시) | — | — |
| 값 입력 + 확정 | edit 모드에서 -/+ 또는 keypad 후 자동 확정 | `edit` → `ok` | — | local validation: 0 이상 |
| edit 취소 | edit 모드에서 ✕ | `edit` → `pending` | — | — |
| "완료" 탭 | 페이지 BigButton | (commit) batch upsert + ← Tab 1 | `POST /stock-snapshots/batch` { snapshots:[ok 카드만] } | 5xx → "잠시 후 다시" toast, 페이지 잔존 |
| 뒤로 가기 | TopBar ← | ← Tab 1 (변경 폐기) | — | — |

**Validation**: 모든 카드가 `pending`이면 "완료" 비활성. 하나라도 `ok`면 활성 (부분 commit 허용).

---

### 4-C. Tab 2 (내 약)

#### S_Cabinet

**진입**: TabBar `내 약` 탭 / 등록 RegDone "약통 둘러보기" / 첫 진입 (empty variant).
**표시 데이터** (from `GET /cabinet`):
- 종류별 그룹 — `prescriptions[]` (Prescription supercards), `otc[]` (Medication rows), `supplements[]` (Medication rows + AnalysisResult.duplicateNutrients flag)
- supp 카드의 `dupe` warning Pill (AnalysisResult derived)

**props**:

| variant | 진입 조건 | 차이점 |
|---|---|---|
| (default) | 활성 medication ≥ 1 | 종류별 그룹 표시 |
| `empty=true` | active medication 0개 | 빈 상태 hero + CTA "약 추가하기" → S_AddType |

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 처방 supercard 탭 | rx 그룹 헤더 탭 | ⬆️ S_RxDetail (sheet) | `GET /prescriptions/{id}` | 404 → toast |
| OTC/supp 카드 탭 | 단독 약 카드 탭 | (편집/상세 — 차기 UI) | `GET /medications/{id}` | — |
| 약 추가 | FAB or empty CTA | → S_AddType | — | — |
| 종류별 sub-tab (처방약/일반의약품/영양제) | 상단 chip 탭 (UI 미정) | filter view 변경 | (client side filter) | — |
| TabBar 이동 | 다른 탭 | → 해당 Tab | — | — |

#### S_RxDetail (⬆️ sheet)

**진입**: S_Cabinet 처방 supercard 탭. backdrop = S_Cabinet 화면 살짝 보임 (dim 0.22).
**표시 데이터**: Prescription joined Medication × IntakeSchedule.

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 닫기 | TopBar ✕ / backdrop 탭 / swipe-to-dismiss | sheet 닫기 ← S_Cabinet | — | — |
| 수정 | footer [수정] 탭 | ⬆️ S_RxEdit (sheet over RxDetail) | `GET /prescriptions/{id}` (form prefill, 이미 있으면 재사용) | — |
| 삭제 | footer [삭제] 탭 | 🔲 S_RxDelete (centered alert) | — | — |
| 약 row 탭 (개별) | 본문 약 row | (단일 약 상세 — 차기) | — | — |

#### S_RxEdit (⬆️ sheet)

**진입**: S_RxDetail [수정] 탭. backdrop = S_RxDetail (Cabinet 그 뒤로 보임).
**표시 데이터**: Prescription form (prescribedAt, clinic 입력) + Medication form rows (이름·dose 입력 + 시간대 toggle Pill) + "약 추가하기" dashed.

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 닫기 | ✕ / backdrop / swipe | ← S_RxDetail (변경 폐기) | — | — |
| 약 추가 | "약 추가하기" dashed 버튼 | inline 빈 row 추가 | — | — |
| 약 제거 | row 우측 ✕ (UI 차기) | inline row 제거 | — | local |
| 시간대 toggle | Pill 탭 | toggle ON/OFF (Pill 컬러 변경 또는 dashed) | — | — |
| 저장 | footer [저장] 탭 | (commit) ← S_RxDetail (refresh) | `PATCH /prescriptions/{id}` { prescribedAt, clinic, medications:[전체 교체] } | 400 validation → inline error · 5xx → "잠시 후 다시" toast |
| 취소 | footer [취소] 탭 | ← S_RxDetail (변경 폐기) | — | — |

**Validation**: 약 0개 → 저장 disable. dose/이름 빈 row → 400.

#### S_RxDelete (🔲 centered alert)

**진입**: S_RxDetail [삭제] 탭. dim 0.5 (alert convention — 본 모달은 backdrop 약하게 처리 안 함, destructive 강조).
**표시 데이터**: trash icon + 처방일·병원·약 N가지 + warning "되돌릴 수 없어요".

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 취소 | [취소] / dim 영역 탭 / OS 백 | ← S_RxDetail (no-op) | — | — |
| 삭제 | [삭제] (danger) | (cascade delete) ← S_Cabinet (refresh) | `DELETE /prescriptions/{id}` | 404 → "이미 삭제됨" toast · 5xx → "잠시 후 다시" |

---

### 4-D. 등록 시나리오 ① 약 등록 (11 화면)

#### S_AddType (🔲 modal)

**진입**: S_Cabinet `+` / Tab 1 empty CTA / Tab 2 add 트리거.
**표시 데이터**: 분기 — "영양제" / "복용의약품" 큰 카드 2개.

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 복용의약품 선택 | 큰 카드 탭 | → S_AddChoice (시나리오 ①) | — | — |
| 영양제 선택 | 큰 카드 탭 | → S_AddChoice (시나리오 ②) | — | — |
| 닫기 | 외부 dim 탭 / OS back | modal 닫기 | — | — |

#### S_AddChoice

**진입**: S_AddType 분기 후. **양 시나리오 공통 단일 카피** ("지금 드시는 약과 영양제를 알려주세요 / 사진이나 이름으로 빠르게 찾을 수 있어요").
**표시 데이터**: 3 옵션 카드 (사진 / 검색 / 직접 입력).

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| "사진으로 추가" | primary 카드 | → S_Camera | (카메라 권한 요청) | 권한 거부 → 직접 입력 안내 |
| "이름으로 찾기" | 보조 카드 | → S_Search | — | — |
| "말로 알려주기" / 직접 | 보조 카드 | → S_Confirm (빈 form) | (마이크 권한) | 권한 거부 → 텍스트 입력 |
| ← | TopBar | ← S_AddType | — | — |

#### S_Camera

**진입**: S_AddChoice "사진으로 추가" + 카메라 권한 OK.
**표시 데이터**: 가이드 사각형 + 샘플 약봉투 미리보기 + 안내 "약봉투를 사각형 안에 맞춰주세요".

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 촬영 | 큰 셔터 버튼 | (OCR 처리 → 결과 받으면) → S_Confirm | `POST /ocr/prescriptions` (multipart, image) — 본 명세 외 추출 service | OCR 실패 → "사진을 다시 찍어 주세요" toast |
| 닫기 | 좌상단 ✕ | ← S_AddChoice | — | — |
| 도움 | 우상단 "도움" | (가이드 — UI 차기) | — | — |

#### S_Confirm

**진입**: S_Camera OCR 응답 / S_AddChoice "직접 입력".
**표시 데이터**: 단일 Card 안에 처방 헤더(병원·약국·조제일·환자) + 약 row 리스트(name·dose·route·복용시점 Pill). 와이어프레임 demo는 `route`(`"1정 · 1일 2회 · 30일분"` — `perDoseAmount + 1일 N회 + durationDays`로 합성)와 `when`(`"식후 30분"` — § 1 ERD `Medication.takingTiming`에 매핑) 두 필드로 분리. API persist 시 서버는 `takingTiming`만 저장하고 `route`는 derive.

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 약 row 탭 (개별 수정) | 약 row | inline edit modal/sheet (UI 차기 — 본 명세 단순) | — | — |
| "N개 모두 추가하기" | 하단 BigButton | (commit Prescription + N Medication) → S_AnalyzeLoading kind=med | `POST /prescriptions` { …Idempotency-Key } | 400 PRESCRIPTION_VALIDATION → inline · 5xx → "잠시 후 다시" |
| ← | TopBar | ← S_Camera or S_AddChoice (변경 폐기) | — | — |

**Validation**: 약 0개 → 버튼 disable. 처방 헤더 필드 빈 칸 → 400.

#### S_AnalyzeLoading (kind=med)

**진입**: S_Confirm "추가하기" 후 자동.
**표시 데이터**: violet ring + capsule + "기존에 드시는 약·영양제와 / 궁합을 확인 중이에요..".

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| (auto) 분석 응답 success | polling success | → S_Result kind=med | `POST /analyses` (202) → `GET /analyses/{id}` polling 3s 간격 | 503 ANALYSIS_TIMEOUT — re-try 자동 1회 후 사용자 안내 |
| (auto) 분석 응답 fail | polling fail | (에러 화면 — UI 차기) → 우회 | — | 424 REFERENCE_DB → "잠시 후 다시" + skip 옵션 |
| ← (사용자 백) | TopBar (구현되면) | ← S_Confirm (Prescription saved 잔존) | — | — |

#### S_Result (kind=med)

**진입**: AnalysisResult `ready`.
**표시 데이터**: 처방 헤더(사랑내과 처방 · 약 3가지 · 5/2) + 복용 시점 caution 카드 (메트포르민+식사) + safe 카드 ("큰 충돌 없음") + footer 면책.

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| "약통에 추가하기" | solid BigButton | → S_Schedule kind=med | — | — |
| "나중에 결정할게요" | ghost BigButton | → S_Schedule kind=med (시니어 — 흐름 막지 않음) | — | — |
| ← | TopBar | (— 시나리오 흐름상 비권장) | — | — |

#### S_Schedule (kind=med)

**진입**: S_Result 통과.
**표시 데이터**: 컨텍스트 chip("사랑내과 처방 · 약 3가지") + 섭취 요일 row("매일 (5/2~5/30 · 30일분)") + 3 시간대 카드 (모두 ON, 약 list).

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 시간대 카드 탭 (영역) | 카드 영역 탭 | ⬆️ S_TimeSheet (slot prop 전달) | — | — |
| 시간대 toggle | 카드 toggle 스위치 | isActive flip (UI 즉시) | (commit은 "완료" 시 batch) | — |
| 섭취 요일 row 탭 | row 카드 | ⬆️ S_DaySheet | — | — |
| "완료" | 하단 BigButton | (commit IntakeSchedule + Frequency) → S_AddNote kind=recommendation | `PATCH /medications/{id}/schedules` × N + `PATCH /medications/{id}/frequency` × N (병렬) | 400 → inline · 5xx → toast |
| ← | TopBar | ← S_Result (변경 폐기 — 단 Prescription·Medication은 이미 saved이므로 schedule 미설정 상태로 잔존) | — | — |

#### S_TimeSheet (⬆️ sheet)

**진입**: S_Schedule 시간대 카드 탭. backdrop = S_Schedule (dim 0.22).
**표시 데이터**: slot 헤더 ("아침 시간 정하기") + 추천 시간 chip (시간대 컬러) + wheel picker (시:분:오전/오후).

**props**: `slot` (`'아침'\|'점심'\|'저녁'`), `kind` (med/supp).

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 휠 스크롤 | wheel 컬럼 | timeOfDay 임시 변경 | — | — |
| "확인" | solid BigButton | (단일 schedule update 즉시 commit) ← S_Schedule (refresh) | `PATCH /schedules/{id}` { timeOfDay } | 5xx → toast, sheet 잔존 |
| "취소" / backdrop / swipe | 외부 영역 / soft button | ← S_Schedule (변경 폐기) | — | — |

**RN 권장**: native iOS DateTimePicker spinner / Android TimePicker로 대체 가능 (§ 3-K 참조).

#### S_DaySheet (⬆️ sheet)

**진입**: S_Schedule 섭취 요일 row 탭. backdrop = S_Schedule.
**표시 데이터**: preset chips 4개 (매일/평일/격일/주말) + 요일 chips 7개 (월~일) selectable.

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| preset chip 탭 | 자주 쓰는 설정 | preset 변경, selectedDays 자동 derive | — | — |
| 요일 chip 탭 | 직접 선택 | preset='custom', selectedDays toggle | — | — |
| "확인" | solid BigButton | (commit Frequency) ← S_Schedule (refresh, dayLabel 갱신) | `PATCH /medications/{id}/frequency` { preset, selectedDays, startDate, endDate? } | 400 (selectedDays 비어있음 등) → inline · 5xx → toast |
| "취소" / backdrop | 외부 / soft button | ← S_Schedule (변경 폐기) | — | — |

#### S_AddNote (kind=recommendation)

**진입**: S_Schedule "완료" 후.
**표시 데이터**: 헤드라인 "약사님이 뭐라고 / 하셨나요?" + 입력 영역 + 음성 입력 보조 + 출처 chips (약사/의사/직접).

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 텍스트 입력 | input 탭 + 키보드 | content 갱신 | — | — |
| 음성 입력 | "말로 입력하기" 탭 | (STT — 본 명세 외) | (STT service) | 권한 거부 → 안내 |
| 출처 chip 선택 | 약사/의사/직접 | source 갱신 | — | — |
| "저장" | BigButton | (commit Note 또는 skip) → S_RegDone | `POST /medications/{id}/notes` { kind:'recommendation', content, source } — content 비어있으면 skip | 400 NOTE_KIND_SOURCE_MISMATCH (source 누락) → inline |
| 건너뛰기 (UI 미정) | (옵션) | → S_RegDone (Note 미저장) | — | — |

#### S_RegDone

**진입**: S_AddNote "저장" 후 — 시나리오 commit 완료.
**표시 데이터**: violet check 132×132 + "저장이 완료됐어요" + "오늘 약속에서 바로 / 드시면 돼요".

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| "오늘 약속 보러 가기" | solid BigButton | → Tab 1 (S_Today) — stack reset | (이미 commit된 상태) | — |
| "약통 둘러보기" | ghost BigButton | → Tab 2 (S_Cabinet) — stack reset | — | — |

**부가**: 본 화면 진입 시 서버는 IntakeSchedule N개 × 시작일~만료일 범위로 Notification 자동 schedule (§ 3-J 참조).

---

### 4-E. 등록 시나리오 ② 영양제 등록 — deltas

시나리오 ②는 ①과 다음 화면 공유: `S_AddType` (영양제 분기) · `S_AddChoice` · `S_Camera` (영양제 라벨 촬영용) · `S_AnalyzeLoading kind=supp` · `S_Result kind=supp` · `S_Schedule kind=supp` · `S_TimeSheet` · `S_DaySheet` · `S_AddNote kind=memo` · `S_RegDone`. 차이는 **kind prop 분기**로 표시·카피만 변경. 본 절은 ②에만 등장하는 7 화면 + variant 차이.

> **scope 명시**: 다음 7 화면(`S_Search` · `S_SearchConfirm` · `S_SearchAdded` · `S_SelectedEmpty` · `S_SelectedList` · `S_SelectedEdit` · `S_DoseSheet`)은 **영양제 시나리오 ② 전용** — 약 등록 시나리오 ①에는 등장하지 않음. variant prop 화면(`S_AnalyzeLoading` · `S_Result` · `S_Schedule` · `S_AddNote`)은 § 4-D 본문 참조 + `kind=supp` 분기 차이만 본 절에서 명시.

#### S_Search

**진입**: S_AddChoice "이름으로 찾기" (영양제 시나리오 — 검색이 주 진입).
**표시 데이터**: 검색 input + 최근 검색 칩 + 결과 카드 list (제품명·브랜드·성분 요약).

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 검색 입력 | textfield + 자동완성 | 결과 list 갱신 (debounced) | `GET /supplements/search?q=` (본 명세 외 reference DB) | rate limit → 안내 |
| 결과 카드 탭 | 큰 카드 | → S_SearchConfirm (modal) | `GET /supplements/{externalId}` | 404 → toast |
| ← | TopBar | ← S_AddChoice | — | — |

#### S_SearchConfirm (🔲 modal)

**진입**: S_Search 결과 탭.
**표시 데이터**: 제품 요약 + 성분·dose 미리보기 + "찾으시는 영양제가 맞나요?".

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| "맞아요" | primary | (selected 임시 list에 추가) ← S_Search 또는 → S_SearchAdded | (in-memory) | — |
| "다른 결과" | secondary | modal 닫기 ← S_Search | — | — |

#### S_SearchAdded

**진입**: S_SearchConfirm "맞아요" 후 (또는 S_Search에서 직접 add 트리거).
**표시 데이터**: 추가된 영양제 카드 (체크 ✓) + 우하단 FAB "다 골랐어요 (N)".

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 추가 더 하기 | ← S_Search 후 반복 | (loop) | — | — |
| FAB "다 골랐어요" | 우하단 FAB | → S_SelectedList (count ≥ 1) 또는 S_SelectedEmpty (count=0 — UI 분기) | — | — |
| 카드 ✓ 해제 | 카드 토글 | selected list에서 제거 | — | — |

#### S_SelectedEmpty

**진입**: count = 0인 상태로 SelectedList 진입 시.
**표시 데이터**: 빈 상태 hero "아직 선택한 영양제가 없어요" + CTA "검색하러 가기" → S_Search.

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 검색하러 가기 | CTA | → S_Search | — | — |
| ← | TopBar | ← S_AddChoice | — | — |

#### S_SelectedList

**진입**: SearchAdded FAB count ≥ 1.
**표시 데이터**: 선택 영양제 N개 카드 (toggle OFF default — dose 설정 필요) + "다음" BigButton.

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| 카드 탭 (활성화) | 카드 영역 | → S_SelectedEdit (해당 카드 toggle ON, dose pill 표시) | — | — |
| "다음" | BigButton | (commit Medication × N) → S_AnalyzeLoading kind=supp | `POST /medications` { items:[…(source=supp)] } | 400 → inline |
| ← | TopBar | ← S_SearchAdded | — | — |

#### S_SelectedEdit

**진입**: S_SelectedList 카드 탭 (또는 S_SelectedList의 일부 변형).
**표시 데이터**: 활성 카드 (toggle ON, dose pill "1정" 표시) + "다음" BigButton.

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| dose pill 탭 | pill | ⬆️ S_DoseSheet | — | — |
| toggle off | 카드 toggle | 비활성화 (제외) | — | — |
| "다음" | BigButton | (commit Medication × N) → S_AnalyzeLoading kind=supp | `POST /medications` { items:[…] } | 동일 |

#### S_DoseSheet (⬆️ sheet)

**진입**: S_SelectedEdit dose pill 탭. backdrop = S_SelectedEdit.
**표시 데이터**: 0.5 / 1(권장) / 1.5 / 2 chip group + "확인".

| 액션 | 트리거 | 다음 | API | 에러 |
|---|---|---|---|---|
| chip 탭 | dose 값 선택 | dose 임시 변경 | — | — |
| "확인" | solid BigButton | (in-memory) ← S_SelectedEdit (refresh) | (commit은 SelectedList "다음"에서 batch) | — |
| 취소 / backdrop | 외부 | ← S_SelectedEdit (변경 폐기) | — | — |

#### S_AnalyzeLoading (kind=supp) — variant delta

§ 4-D S_AnalyzeLoading와 동일하되 카피: "박정숙님의 영양제를 / 약과 궁합 확인중..". `POST /analyses` { kind:'supp', triggerMedicationIds }.

#### S_Result (kind=supp) — variant delta

§ 4-D S_Result와 동일 골격. 차이:
- 헤더: GNM 로고 + "오메가-3 알티지" + EPA+DHA 600mg
- 본문: 출혈 warning 카드 (아스피린 + 오메가-3) + 비타민 D 중복 카드 (1,200 IU / 2,000 IU 한도 내)
- AnalysisResult.duplicateNutrients 표시 — `kind='med'` 시 hidden

#### S_Schedule (kind=supp) — variant delta

§ 4-D S_Schedule와 동일 골격. 차이:
- 컨텍스트 chip: "영양제 1가지"
- 섭취 요일 default: "매일" (durationDays 없음)
- 시간대 default: 저녁만 ON 예시 (`isActive=true`로 supp 카드만)

#### S_AddNote (kind=memo) — variant delta

§ 4-D S_AddNote와 동일 골격. 차이:
- 헤드라인: "이 영양제에 대해 / 메모하실래요?"
- 출처 chips **숨김** (memo는 source=null)
- 안내 카드: "메모는 본인만 볼 수 있어요. 의료인 권고는 약 등록에서 입력해 주세요."
- API: `POST /medications/{id}/notes` { kind:'memo', content, source:null }

---

### 4-F. 화면별 검증 체크 (커버리지)

| § | 영역 | 화면 수 | variant 행 |
|---|---|---|---|
| 4-B | Tab 1 | 2 | S_Today × 4 + S_StockCheck = 5 |
| 4-C | Tab 2 | 4 | S_Cabinet × 2 + RxDetail + RxEdit + RxDelete = 5 |
| 4-D | 등록 ① 약 | 11 | 11 (variant 모두 한 row씩) |
| 4-E | 등록 ② 영양제 | 17 (공유 + delta) | 7 신규 + 4 variant delta = 11 (나머지 6은 ① 참조) |
| **합계** | **약 21 + 등록 11/17** | — | **§ 4 본문 32 row** |

명시적 제외 (§ 0 reaffirm): Tab 3 마이페이지·공유·리포트, Caregiver, ENTRY 8 화면, EXPLORE 데모.

### 4-G. 후속 layer 연결 (RN 단계 검증)

본 매트릭스는 RN 구현 시 다음 검증에 그대로 사용:
1. 각 row의 "API" 컬럼이 § 3에 정의된 endpoint와 1:N 매핑되는지
2. "전이" 컬럼이 § 2 상태 머신의 from→to와 일치하는지
3. "표시 데이터"의 entity field가 § 1 ERD에 존재하는지

후속 작업으로 본 명세서는 v2에서 (a) Tab 3 + 보호자 추가, (b) 에러 화면 자세히 명시, (c) 접근성 (VoiceOver/TalkBack) row 추가 가능.

